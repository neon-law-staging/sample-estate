name: ci

on:
  pull_request:

jobs:
  ci:
    uses: neon-law-source-code/navigator/.github/workflows/project-gate.yml@26.9.21
    with:
      project: "sample-estate"
      host: "staging.neonlaw.com"
